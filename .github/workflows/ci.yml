name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  # Keep PR supersession; never cancel in-flight main runs (reruns/outages were wiping them).
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  quality:
    name: Typecheck · Lint · Test · Build
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        node-version: ["20", "22"]
        include:
          - os: windows-latest
            node-version: "20"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Build
        run: npm run build

      - name: Test
        shell: bash
        run: |
          mkdir -p test-results
          npm test -- --reporter=default --reporter=json --outputFile=test-results/results.json

      - name: Pack dry-run
        run: npm pack --dry-run

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.os }}-node-${{ matrix.node-version }}
          path: test-results/
          if-no-files-found: ignore

      - name: Upload build artifact
        if: success() && matrix.os == 'ubuntu-latest' && matrix.node-version == '20'
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  action-smoke:
    name: GitHub Action smoke · ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: quality
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: normal output
            case: normal
            output-file: test-results/action-report.json
            expected: success
            path: fixtures/clean-configured-project
            version: 1.0.0
            minimum-score: ""
            fail-on-severity: ""
            expected-outcome: success
            summary: "false"
            annotations: "false"
          - name: nested output
            case: nested
            output-file: test-results/nested/action-report.json
            expected: success
            path: fixtures/clean-configured-project
            version: 1.0.0
            minimum-score: ""
            fail-on-severity: ""
            expected-outcome: success
            summary: "false"
            annotations: "false"
          - name: traversal rejected
            case: traversal
            output-file: ../report.json
            expected: failure
            path: fixtures/clean-configured-project
            version: 1.0.0
            minimum-score: ""
            fail-on-severity: ""
            expected-outcome: ""
            summary: "false"
            annotations: "false"
          - name: escaping parent symlink rejected
            case: parent-symlink
            output-file: test-results/escape-parent/report.json
            expected: failure
            path: fixtures/clean-configured-project
            version: 1.0.0
            minimum-score: ""
            fail-on-severity: ""
            expected-outcome: ""
            summary: "false"
            annotations: "false"
          - name: output symlink rejected
            case: output-symlink
            output-file: test-results/output-symlink.json
            expected: failure
            path: fixtures/clean-configured-project
            version: 1.0.0
            minimum-score: ""
            fail-on-severity: ""
            expected-outcome: ""
            summary: "false"
            annotations: "false"
          - name: directory rejected
            case: directory
            output-file: test-results/output-directory
            expected: failure
            path: fixtures/clean-configured-project
            version: 1.0.0
            minimum-score: ""
            fail-on-severity: ""
            expected-outcome: ""
            summary: "false"
            annotations: "false"
          - name: policy min-score fails
            case: policy-min-score
            output-file: test-results/policy-min-score.json
            expected: failure
            path: fixtures/insecure-agent-project
            version: workspace
            minimum-score: "90"
            fail-on-severity: ""
            expected-outcome: policy-failure
            summary: "true"
            annotations: "true"
          - name: policy severity fails
            case: policy-severity
            output-file: test-results/policy-severity.json
            expected: failure
            path: fixtures/insecure-agent-project
            version: workspace
            minimum-score: ""
            fail-on-severity: critical
            expected-outcome: policy-failure
            summary: "true"
            annotations: "true"
          - name: policy clean passes
            case: policy-clean
            output-file: test-results/policy-clean.json
            expected: success
            path: fixtures/clean-configured-project
            version: workspace
            minimum-score: "90"
            fail-on-severity: critical
            fail-on-rule: ""
            verify-baseline: ""
            fail-on-new: ""
            expected-outcome: success
            summary: "true"
            annotations: "true"
          - name: policy fail-on-rule fails
            case: policy-fail-on-rule
            output-file: test-results/policy-fail-on-rule.json
            expected: failure
            path: fixtures/insecure-agent-project
            version: workspace
            minimum-score: ""
            fail-on-severity: ""
            fail-on-rule: security/env-file-exposure
            verify-baseline: ""
            fail-on-new: ""
            expected-outcome: policy-failure
            summary: "true"
            annotations: "false"
          - name: verify-baseline clean
            case: verify-baseline-clean
            output-file: test-results/verify-baseline-clean.json
            expected: success
            path: fixtures/clean-configured-project
            version: workspace
            minimum-score: ""
            fail-on-severity: ""
            fail-on-rule: ""
            verify-baseline: test-results/verify-baseline-source.json
            fail-on-new: "true"
            expected-outcome: success
            summary: "false"
            annotations: "false"
          - name: verify-baseline symlink escape rejected
            case: verify-baseline-symlink
            output-file: test-results/verify-baseline-symlink.json
            expected: failure
            path: fixtures/clean-configured-project
            version: workspace
            minimum-score: ""
            fail-on-severity: ""
            fail-on-rule: ""
            verify-baseline: test-results/verify-baseline-escape.json
            fail-on-new: ""
            expected-outcome: ""
            summary: "false"
            annotations: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifact
        if: matrix.version == 'workspace'
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Install runtime dependencies for workspace CLI
        if: matrix.version == 'workspace'
        run: npm ci --omit=dev

      - name: Prepare output-path case
        env:
          CASE: ${{ matrix.case }}
        run: |
          set -euo pipefail
          mkdir -p test-results
          case "$CASE" in
            parent-symlink)
              ln -s "$RUNNER_TEMP" test-results/escape-parent
              ;;
            output-symlink)
              ln -s "$RUNNER_TEMP/outside-report.json" test-results/output-symlink.json
              ;;
            directory)
              mkdir -p test-results/output-directory
              ;;
            verify-baseline-clean)
              node dist/cli/index.js fixtures/clean-configured-project --json > test-results/verify-baseline-source.json
              ;;
            verify-baseline-symlink)
              echo '{"outside":true}' > "$RUNNER_TEMP/outside-baseline.json"
              ln -s "$RUNNER_TEMP/outside-baseline.json" test-results/verify-baseline-escape.json
              ;;
          esac

      - name: Run AgentDoctor action
        id: agentdoctor
        continue-on-error: ${{ matrix.expected == 'failure' }}
        uses: ./
        with:
          path: ${{ matrix.path }}
          output-file: ${{ matrix.output-file }}
          version: ${{ matrix.version }}
          minimum-score: ${{ matrix.minimum-score }}
          fail-on-severity: ${{ matrix.fail-on-severity }}
          fail-on-rule: ${{ matrix.fail-on-rule }}
          verify-baseline: ${{ matrix.verify-baseline }}
          fail-on-new: ${{ matrix.fail-on-new }}
          summary: ${{ matrix.summary }}
          annotations: ${{ matrix.annotations }}

      - name: Validate expected step outcome
        env:
          ACTUAL_OUTCOME: ${{ steps.agentdoctor.outcome }}
          EXPECTED_OUTCOME: ${{ matrix.expected }}
        run: |
          if [ "$ACTUAL_OUTCOME" != "$EXPECTED_OUTCOME" ]; then
            echo "expected $EXPECTED_OUTCOME, got $ACTUAL_OUTCOME" >&2
            exit 1
          fi

      - name: Validate action outcome output
        if: matrix.expected-outcome != ''
        env:
          ACTUAL: ${{ steps.agentdoctor.outputs.outcome }}
          EXPECTED: ${{ matrix.expected-outcome }}
        run: |
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "expected outcome=$EXPECTED, got $ACTUAL" >&2
            exit 1
          fi

      - name: Validate containment
        if: matrix.expected == 'failure' && (matrix.version != 'workspace' || matrix.case == 'verify-baseline-symlink')
        env:
          CASE: ${{ matrix.case }}
        run: |
          set -euo pipefail
          case "$CASE" in
            parent-symlink)
              test ! -e "$RUNNER_TEMP/report.json"
              ;;
            output-symlink)
              test ! -e "$RUNNER_TEMP/outside-report.json"
              ;;
            verify-baseline-symlink)
              test ! -e test-results/verify-baseline-symlink.json
              ;;
          esac

      - name: Validate JSON report
        if: matrix.expected == 'success' || startsWith(matrix.case, 'policy-') || matrix.case == 'verify-baseline-clean'
        env:
          REPORT_PATH: ${{ steps.agentdoctor.outputs.report-path }}
          VERSION_MODE: ${{ matrix.version }}
          CASE: ${{ matrix.case }}
        run: |
          node <<'NODE'
          const fs = require("node:fs");

          const report = JSON.parse(fs.readFileSync(process.env.REPORT_PATH, "utf8"));
          if (process.env.VERSION_MODE !== "workspace" && report.version !== "1.0.0") {
            throw new Error(`unexpected AgentDoctor version: ${report.version}`);
          }
          if (process.env.CASE === "verify-baseline-clean") {
            if (!report.summary || typeof report.summary.new !== "number") {
              throw new Error("verify report missing summary.new");
            }
            if (report.summary.new !== 0) {
              throw new Error(`expected 0 new findings, got ${report.summary.new}`);
            }
          } else if (
            !Array.isArray(report.findings) ||
            report.scoringAvailable !== true ||
            report.scores == null ||
            typeof report.scores.overall !== "number"
          ) {
            throw new Error("action report does not match the current JSON contract");
          }
          NODE

      - name: Upload AgentDoctor report
        if: matrix.expected == 'success' || startsWith(matrix.case, 'policy-') || matrix.case == 'verify-baseline-clean'
        uses: actions/upload-artifact@v4
        with:
          name: agentdoctor-report-${{ matrix.case }}
          path: ${{ matrix.output-file }}
          if-no-files-found: error

  project-brain:
    name: Project Brain laboratory
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: quality
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Understanding unit tests
        run: npm run test:understanding

      - name: Understanding validation
        run: npm run validate:understanding

      - name: Project Brain validation
        run: npm run validate:project-brain

      - name: Project Brain benchmark
        run: npm run benchmark:project-brain
